import React from "react";
import "./Title.css";

const Title = props => 
<div className="title">
<header>
    <h1>Clicky Game</h1>
    <h4>Click on an image to earn points, but don't click on an image more than once!</h4>
</header>
    
</div>

export default Title;